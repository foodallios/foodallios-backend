import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'foodallios.backend',
  appName: 'foodallios-backend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
